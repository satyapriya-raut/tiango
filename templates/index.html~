<!DOCTYPE HTML>
<html>
	<head>
		<title>Login</title>
		<!--<link href="../static/css/login.css" rel="stylesheet"> -->
		<link rel="stylesheet" href="{{ url_for('static', filename='css/login.css') }}">
		<script type="text/javascript">
   		function validateForm(){
        let text = this.getElementById('pass').value;
        text = text.trim();
        text = text.split(" "); 
        return (text.length == 1); 
    }
</script>
	</head>
<style>
h3{
	color: red;
}
</style>
	<body>
	
	
	<div id='container'>
	<h1>Welcome to Tiango Quiz!!</h1>
	<h2 align="center">User Login</h2>
  <div class="signup">
     <form action="{{ url_for('validate') }}" method="POST">
       <input type="text" placeholder="User ID:" name="userID" title="Enter your User ID of min 4 characters. Space not allowed" minlength="4" maxlength="16" pattern="^\S+$" required />
       <input type="password" placeholder="Password:" name="pwd" id="pass" title="Enter password of min 6 characters. No space allowed" minlength="6" maxlength="16" pattern="^\S+$" required />
       <input type="submit" value="Login" placeholder="SUBMIT" />
		   {% with messages = get_flashed_messages() %}
			  {% if messages %}
				{% for message in messages %}
				  <h3>{{ message }}</h3>
				{% endfor %}
			  {% endif %}
			{% endwith %}
       <p>Don't have an account. <a href="{{ url_for('signupPage') }}">Register</a></p> 
       
     </form>
  </div>
</div>

	
	
	
	</body>
</html>
